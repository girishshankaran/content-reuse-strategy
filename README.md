# content-reuse-strategy

Release asset branch for `20.0`.

This branch contains packaging only:

- `manifests/book.yml`
- `assets/toc.yml`
- `assets/release-metadata.yml`

Canonical topics remain on the `main` branch.
